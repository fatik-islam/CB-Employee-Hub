<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chicky Bites Employee Hub</title>
    <link rel="icon" type="image/png" href="/assets/logo.png" />
    <link rel="shortcut icon" type="image/png" href="/assets/logo.png" />
    <link rel="apple-touch-icon" href="/assets/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700;800&family=Manrope:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/public/css/styles.css" />
  </head>
  <body class="login-screen">
    <div class="ambient-bg"></div>

    <main class="auth-card">
      <div class="brand brand-auth">
        <span class="brand-mark brand-mark-cb">
          <img src="/assets/logo.png" alt="Chicky Bites logo" class="brand-logo" />
        </span>
        <div>
          <h1>Kiosk Access</h1>
        </div>
      </div>

      <% if (flash) { %>
        <div
          id="flashToastData"
          hidden
          data-flash-type="<%= flash.type %>"
          data-flash-message="<%= flash.message %>"
        ></div>
      <% } %>

      <form action="/kiosk-login" method="post" class="stack-form">
        <label>
          Kiosk PIN
          <input type="password" name="pin" inputmode="numeric" autocomplete="one-time-code" required />
        </label>

        <button class="btn primary" type="submit">Enter Attendance Mode</button>
      </form>

      <div class="split-actions mt-10">
        <a class="btn neutral" href="/login">Admin Sign In</a>
      </div>
    </main>
    <script src="/public/js/toast.js"></script>
  </body>
</html>
